// components/common/Spinner.js
import { FaSpinner } from "react-icons/fa";

const Spinner = () => (
  <div className="flex justify-center items-center">
    <FaSpinner className="animate-spin text-2xl text-blueColor" />
  </div>
);

export default Spinner;
